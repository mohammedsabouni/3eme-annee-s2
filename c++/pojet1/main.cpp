#include<iostream>
#include"Point.hpp"
#include"Point3.hpp"

int main(){
    // Point A, *B, C(1,5,"cc"), D(A);
    //const Point C(2,3,"ABC");
    // A.affiche();
    // B = new Point(1.3, "BBB");
    // A.affiche();
    // A.saisie();
    // B->saisie();
    // A.affiche();
    // B->affiche();
    // C.affiche();
    // if (B->compare(A))
    // {
    //     std::cout << "egalite" << std::endl;
    // }
    // else{
    //     std::cout << "non egeaux" << std::endl;
    // }
    // D = A + C;
    // D.affiche();
    // (D*2).affiche();
    // (3*D).affiche();
    // std::cin>>D;
    // std::cout << D << std::endl;
    // delete B;
    // std::cout << "*********" << std::endl;


    Point3 S;
    return 0;
}